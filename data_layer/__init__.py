"""Phase 1 historical data layer: pull, store, sanity-check, and slice bars.

`store.py` is the only network-touching module. `sanity.py` and `slice.py`
are pure and run fully offline (brief L10).
"""
